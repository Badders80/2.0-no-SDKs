// src/components/Hero.tsx
import React from 'react';

export default function Hero() {
  return (
    <section className="w-full h-screen bg-black flex items-center justify-center">
      {/* Intentionally blank — ready for future content */}
    </section>
  );
}
